import React from 'react';

const styles = {
    footer: {
        minHeight: '40px',
        background: 'red',
        // position: 'absolute'
    }
}

const Footer = (props) => (
        <div style={styles.footer}>
        </div>
);

export default Footer;
